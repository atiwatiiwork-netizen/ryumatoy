-- ============================================================================
-- Ryuma — v68: แพลตฟอร์มที่อยู่จัดส่ง (เจ้าของ 2026-09-12) · วางใน SQL Editor แล้วกด Run
-- รันซ้ำได้ · ไม่ทำข้อมูลเสีย · เพิ่มคอลัมน์เดียว ไม่แตะ RLS/trigger เดิม
--
-- users.shipping_info (jsonb) = ที่อยู่แบบแยกช่อง { name, phone, address, province, postal }
--   · ชื่อ/เบอร์ในนี้คือ "ผู้รับพัสดุ" — แยกจากชื่อเฟส (display_name) และเบอร์ล็อกอิน (phone ซึ่ง
--     guard คุ้มครอง ลูกค้าแก้เองไม่ได้) → ลูกค้าแก้ที่อยู่เองได้ไม่ชนด่าน
--   · shipping_address (ข้อความเดิม) ยังถูกเขียนคู่ทุกครั้งที่เซฟ — ใบปะหน้า/หน้าจัดส่ง/แอดมินใช้ต่อได้
--   · RLS: users_own ครอบอยู่แล้ว (แถวตัวเอง) · guard_user_columns ไม่คุมคอลัมน์นี้ = ตั้งใจให้แก้ได้
--
-- ⚠ รันก่อนหรือทันทีหลัง deploy — ถ้าลูกค้าเซฟที่อยู่ก่อนคอลัมน์มี การเซฟจะค้าง "กำลังส่งขึ้นระบบ"
--   แล้วสำเร็จเองทันทีที่รันไฟล์นี้ (ระบบ retry ให้อัตโนมัติ ไม่มีข้อมูลหาย)
-- ============================================================================

alter table users add column if not exists shipping_info jsonb;

-- ── ตรวจหลังรัน (ไม่บังคับ) ──────────────────────────────────────────────────
-- select column_name from information_schema.columns
--   where table_name = 'users' and column_name = 'shipping_info';  → ต้องเจอ 1 แถว
